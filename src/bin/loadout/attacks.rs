use snowball_fight::bezier::BezierCurve;
use bevy::prelude::*;

use crate::loadout::components::*;
use crate::loadout::actions::*;
/*
pub const NormalAttack: AttackLoadout = 
    AttackLoadout {
        damage: 1.0,
        cooldown: 0.5,
        duration: 0.5,
        action: spawn_snowball,
        path: t
    };
 */   
